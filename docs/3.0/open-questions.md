# 3.0 — Open questions (appendix)

> **Status:** residual fog only — **not** blocking package complete  
> Major axes are locked in mandatory chapters. Items below are intentional Later
> or implementation-time details.

## Deferred product (Later / out of 3.0 Must)

- Finder `NSFileProvider` integration  
- Embedded mirror (vs external scrcpy)  
- Multi-window / window-per-device  
- Full VoiceOver certification as ship gate (progressive Should remains)  
- Audio forwarding  
- 0.2 → 3.0 settings & transfer-history migration details (see optional [migration.md](migration.md))

## Implementation-time (not product grilling)

- Exact DesignSystem spacing token table in code  
- Whether to extract optional `DroidMateUI` SPM target  
- Feature-flag strategy while shell lands  
- What's New / marketing copy for launch  

When an item becomes a hard product decision, open a new effort — do not silently
re-fog locked chapters.
